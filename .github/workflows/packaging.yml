name: Packaging (Linux — Flatpak)

# Verifies the Flatpak build as a required signal. This developer manifest
# grants build-only network for Cargo and the statically linked ONNX Runtime
# archive. Cargo.lock checksums every crate and ort-sys verifies its pinned
# archive SHA256, so changing bytes fail closed even though transport is online.
# Flathub submission still requires expressing those inputs as offline sources.

on:
  push:
    branches: [main]
    paths:
      - 'packaging/**'
      - 'platforms/linux/**'
      - 'platforms/windows/src/engine/**'
      - '.github/workflows/packaging.yml'
  pull_request:
    paths:
      - 'packaging/**'
      - 'platforms/linux/**'
      - 'platforms/windows/src/engine/**'
      - '.github/workflows/packaging.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Flatpak — the primary, universal channel. Installs the GNOME 46 runtime +
  # the rust-stable SDK extension (GNOME 46 is based on freedesktop-sdk 23.08),
  # then runs flatpak-builder against packaging/flatpak/.
  # ---------------------------------------------------------------------------
  flatpak:
    name: Flatpak (GNOME 46) — x64
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - name: Install flatpak + flatpak-builder
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends flatpak flatpak-builder

      - name: Add Flathub remote
        run: |
          flatpak remote-add --if-not-exists --user flathub \
            https://flathub.org/repo/flathub.flatpakrepo

      - name: Install GNOME 46 runtime + rust-stable SDK extension
        run: |
          set -euo pipefail
          flatpak install -y --user flathub \
            org.gnome.Platform//46 \
            org.gnome.Sdk//46 \
            org.freedesktop.Sdk.Extension.rust-stable//23.08

      - name: Build the Flatpak
        # --share=network is granted to the build step inside the developer
        # manifest for Cargo + ort's static archive; nothing is uploaded. The
        # shipped sandbox's network grant remains user-initiated HF downloads.
        run: |
          set -euo pipefail
          flatpak-builder --user --force-clean --disable-rofiles-fuse \
            --repo=repo \
            build-dir \
            packaging/flatpak/io.github.fileid.FileID.yaml

      - name: Bundle the .flatpak (artifact)
        run: |
          set -euo pipefail
          flatpak build-bundle repo FileID.flatpak io.github.fileid.FileID
          sha256sum FileID.flatpak > FileID.flatpak.sha256
          sha256sum --check FileID.flatpak.sha256

      - name: Upload Flatpak bundle
        uses: actions/upload-artifact@v4
        with:
          name: fileid-flatpak
          path: |
            FileID.flatpak
            FileID.flatpak.sha256
          if-no-files-found: error
