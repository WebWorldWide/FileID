<?xml version="1.0" encoding="utf-8"?>
<!--
    SidebarQueueList — pending background jobs (scan / faceCluster /
    deepAnalyze). Hidden when the queue is empty. Each row: category icon,
    title, optional ETA. Total ETA at the top.


-->
<UserControl
    x:Class="FileID.Views.Sidebar.SidebarQueueList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel x:Name="Root" Spacing="8" Visibility="Collapsed">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="UP NEXT"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       FontWeight="SemiBold"
                       CharacterSpacing="40"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                       AutomationProperties.HeadingLevel="Level2" />
            <TextBlock x:Name="TotalEtaText"
                       Grid.Column="1"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
        </Grid>

        <ScrollViewer MaxHeight="280"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalScrollMode="Disabled">
            <ItemsRepeater x:Name="JobsRepeater">
                <ItemsRepeater.Layout>
                    <StackLayout Spacing="4" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="8,6"
                              CornerRadius="8"
                              Background="{Binding Background}"
                              AutomationProperties.Name="{Binding AutomationName}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0"
                                      Margin="0,0,8,0"
                                      FontFamily="Segoe Fluent Icons"
                                      Glyph="{Binding IconGlyph}"
                                      FontSize="11"
                                      Opacity="{Binding IconOpacity}" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Title}"
                                       FontSize="11"
                                       FontWeight="{Binding TitleWeight}"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding Eta}"
                                       FontSize="10"
                                       Opacity="0.5"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </StackPanel>
</UserControl>
