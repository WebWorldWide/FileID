<?xml version="1.0" encoding="utf-8"?>
<!--
    SettingToggleRow — title + optional subtitle on the left, gold-tinted
    toggle on the right. Mirror of macOS SettingToggleRow (Theme.swift:88).
    The subtitle wraps; the toggle is right-aligned and gold-tinted.

    Usage:
        <theme:SettingToggleRow
            Title="Tag kept files after Cleanup"
            Subtitle="Adds a 'kept' tag to the file you keep after a duplicate trash."
            IsOn="{x:Bind ViewModel.AutoTagKept, Mode=TwoWay}" />
-->
<UserControl
    x:Class="FileID.Theme.Controls.SettingToggleRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid ColumnSpacing="12" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Spacing="2" VerticalAlignment="Center">
            <TextBlock x:Name="TitleText"
                       FontSize="{StaticResource FontSizeCaption}"
                       FontWeight="SemiBold" />
            <TextBlock x:Name="SubtitleText"
                       FontSize="10"
                       Opacity="0.55"
                       TextWrapping="Wrap"
                       Visibility="Collapsed" />
        </StackPanel>

        <ToggleSwitch x:Name="Switch"
                      Grid.Column="1"
                      OnContent=""
                      OffContent=""
                      MinWidth="0"
                      VerticalAlignment="Center"
                      Toggled="OnSwitchToggled" />
    </Grid>
</UserControl>
