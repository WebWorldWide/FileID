# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Swift / SPM
/.build
.build/
/Packages
/*.xcodeproj
/*.xcworkspace
.swiftpm/

# Built app bundle (assembled by run.sh; glob covers Finder-duplicated copies).
/FileID*.app/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Model weights — these are downloaded at runtime, not source artifacts.
# Anything under Resources that is code stays tracked; large binary weight
# formats are excluded wholesale.
*.safetensors
*.gguf
*.mlmodel
*.mlmodelc
*.mlpackage
*.onnx
*.pt
*.pth
*.bin
*.ckpt
*.tflite
*.weights
Resources/Models/
Resources/**/weights/
Resources/**/*.safetensors

# Test corpus — auto-generated by scripts/build_corpus.sh, not redistributed.
# Keep README.md so the folder structure stays documented.
Tests/Corpus/*
!Tests/Corpus/README.md

# Python venv used by scripts/convert_arcface.py + scripts/test_assertions.py
.venv/

# Test driver compiled artifact
scripts/make_bookmark

# Claude Code session state + local settings.
.claude/

# Zed editor personal settings.
.zed/
.zed*

# Personal scripts hardcoded to the maintainer's NAS path / home.
# Re-add per-developer copies if needed; don't commit them.
scripts/iterate_truenas.sh
scripts/perfharness.swift
scripts/deepharness.swift

# Local user overrides — never commit.
*.local.json
.env
.env.local

# DMG build output.
dist/
